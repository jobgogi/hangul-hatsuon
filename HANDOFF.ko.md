# 인수인계·유지보수 문서

대상: 한글 발음 변환 서비스 (hangul-hatsuon)

## 1. 서비스 개요

한글을 읽기 어려운 일본인을 돕는 서비스. 상세 기능 목록·아키텍처는 `CLAUDE.md`를 참조. 요점만 다시 정리한다.

- 한글 문장을 카타카나/로마자 발음 표기로 변환한다.
- Web Speech API의 TTS로 한국어(원문)를 재생할 수 있다.
- 음절별로 카타카나 발음을 나누어 표시해 가독성을 높인다.
- TTS로 카타카나 표기 그대로의 발음(일본어 음성)을 재생할 수 있다.

## 2. 기술 스택

- React 19 + TypeScript + Vite
- ESLint (`eslint.config.js`)
- 배포: `gh-pages` 패키지로 `dist/`를 `deploy` 브랜치에 게시 (GitHub Pages 프로젝트 사이트로 `/hangul-hatsuon` 경로 하위에서 서빙)
- 테스트: Vitest (`src/lib/`의 변환 로직만 대상. UI 레이어 테스트는 없음)

## 3. 개발·운영 명령어

- `npm run dev` — 개발 서버 실행
- `npm run build` — 타입 체크(`tsc -b`) + 빌드(`vite build`)
- `npm run lint` — ESLint 검사
- `npm run preview` — 프로덕션 빌드 미리보기
- `npm run deploy` — 빌드 후 `dist/`를 `deploy` 브랜치에 게시
- `npm test` — `src/lib/`의 유닛 테스트(Vitest) 실행

배포 경로는 `vite.config.ts`의 `base: '/hangul-hatsuon'`으로 고정되어 있다. 저장소 이름이나 호스팅 위치를 변경할 경우 이 부분도 함께 수정해야 한다.

## 4. 아키텍처 (요약)

변환 로직은 `src/lib/`에 모여 있으며 UI와는 독립적이다. 처리 흐름:

1. 입력 텍스트를 줄 단위로 나누고, 다시 공백·문장부호로 끊기지 않는 "run"(연속된 한글 덩어리) 단위로 분할한다.
2. `HangulDecomposer`가 각 음절을 초성/중성/종성으로 분해한다.
3. `LiaisonProcessor`가 우선순위가 정해진 규칙으로 연음·음운 변화(구개음화, 유음화, 비음화, 격음화, ㅎ탈락, 겹받침 분리, 연음, 어말 대표음화 등)를 적용한다. 규칙 적용 순서에 의존하는 구현이므로, 수정 시 기존 우선순위를 깨지 않도록 주의해야 한다.
4. `SyllableKanaMapper`가 음운 처리가 끝난 음절을 카타카나/로마자로 변환한다. 파일 앞부분의 행·모음 테이블이 매핑의 원본(source of truth)이다.
5. `known-words.ts`의 예외 사전이 규칙대로 변환하면 부자연스러워지는 정착된 외래어 표기를 덮어쓴다.
6. UI 레이어(`App.tsx` / `src/components/` / `useHangulToKana`)가 위 변환 결과와 TTS 재생(`speechSynthesis`)을 화면에 연결한다.

각 파일의 역할·확장 방법에 대한 자세한 내용은 `CLAUDE.md`를 참조.

## 5. 알려진 제약·한계

- **카타카나 표기는 근사치**: ㅓ/ㅗ, ㅡ/ㅜ, 평음/격음 구분은 카타카나로 표현할 수 없다(`App.tsx` 하단 안내문에도 명시되어 있음). 정확한 발음은 반드시 TTS 재생으로 확인하는 것을 전제로 한 설계다.
- **예외 사전은 주로 한식 단어 위주**: `KNOWN_WORDS`에는 찌개·짜장면·김치·떡볶이·비빔밥·불고기·김밥·삼계탕·잡채·삼겹살 등이 등록되어 있다. 다만 전체를 다루지는 못하므로, 규칙대로 변환하면 부자연스러워지는 단어를 발견할 때마다 수시로 추가가 필요하다.
- **연음은 run 단위로 한정**: 공백·문장부호를 넘나드는 음운 변화(복합어 경계 등)는 처리하지 않는 사양이다.
- **TTS는 기기·브라우저 의존적**: Web Speech API에 한국어(`ko-KR`)/일본어(`ja-JP`) 음성이 설치되어 있지 않은 환경에서는 재생할 수 없다. `voiceReady`/`hasKo`로 경고 문구만 표시할 뿐, 대체 수단(외부 음성 합성 API 이용 등)은 구현되어 있지 않다.
- **테스트는 lib만 존재**: `src/lib/`의 각 클래스에 대한 Vitest 유닛 테스트는 정비되어 있지만(`npm test`), UI 레이어(`App.tsx`/컴포넌트/훅)에는 테스트가 없다. `LiaisonProcessor`는 규칙 우선순위에 강하게 의존하므로, 수정 시 테스트와 함께 수동 확인도 권장한다.
- **IME·특수 입력 동작은 미검증**: 결합 문자나 이체자 등, 표준 한글 음절(U+AC00~U+D7A3) 범위 밖의 입력에 대한 동작은 확인되지 않았다.

## 6. 향후 개선 포인트 (제안)

- `KNOWN_WORDS` 사전 확충, 또는 외부 데이터화(JSON 분리 등)를 통한 업데이트 편의성 향상.
- 음절 칩 UI의 접근성 개선(키보드 조작, 스크린 리더 대응 aria 속성 등).
- 재생 속도(`rate`) 외의 음성 파라미터(pitch 등) 조절 UI 검토.
- TTS 미지원 환경을 위한 대안(외부 TTS API 이용 등) 검토.
