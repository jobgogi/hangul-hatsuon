# 引き継ぎ・保守ドキュメント

対象: ハングル発音変換サービス（hangul-hatsuon）

## 1. サービス概要

ハングルを読みにくい日本人を手伝うサービス。詳細な機能一覧・アーキテクチャは `CLAUDE.md` を参照。要点のみ再掲する。

- ハングル文章をカタカナ／ローマ字の発音表記に変換する。
- Web Speech APIのTTSで韓国語（原文）を再生できる。
- 音節ごとにカタカナ発音を分けて表示し、視認性を高める。
- TTSでカタカナ表記通りの発音（日本語音声）を再生できる。

## 2. 技術スタック

- React 19 + TypeScript + Vite
- ESLint（`eslint.config.js`）
- デプロイ: `gh-pages`パッケージで`dist/`を`deploy`ブランチへ公開（GitHub Pagesのプロジェクトサイトとして`/hangul-hatsuon`配下で配信）
- テスト: Vitest（`src/lib/`の変換ロジックのみ対象。UI層のテストは無し）

## 3. 開発・運用コマンド

- `npm run dev` — 開発サーバー起動
- `npm run build` — 型チェック（`tsc -b`）＋ビルド（`vite build`）
- `npm run lint` — ESLintチェック
- `npm run preview` — 本番ビルドのプレビュー
- `npm run deploy` — ビルド後、`dist/`を`deploy`ブランチに公開
- `npm test` — `src/lib/`のユニットテスト（Vitest）を実行

デプロイ先のパスは `vite.config.ts` の `base: '/hangul-hatsuon'` で固定されている。リポジトリ名やホスティング先を変更する場合は、ここも合わせて修正する必要がある。

## 4. アーキテクチャ（要約）

変換ロジックは `src/lib/` に集約され、UIとは独立している。処理の流れ：

1. 入力テキストを行ごとに分割し、さらに空白・句読点で区切られない「run」（連続したハングルの塊）に分割する。
2. `HangulDecomposer` が各音節を初声／中声／終声に分解する。
3. `LiaisonProcessor` が優先順位付きのルールで連音・音韻変化（口蓋音化、流音化、鼻音化、激音化、ㅎ脱落、重終声分離、連音、語末代表音化など）を適用する。ルールの適用順序に依存した実装のため、変更時は既存の優先順位を崩さないよう注意する。
4. `SyllableKanaMapper` が音韻処理後の音節をカタカナ／ローマ字に変換する。行・母音のテーブル（ファイル冒頭）がマッピングの正本。
5. `known-words.ts` の例外辞書で、規則通りに変換すると不自然になる定着済み外来語表記を上書きする。
6. UI層（`App.tsx` / `src/components/` / `useHangulToKana`）が上記の変換結果とTTS再生（`speechSynthesis`）を画面に結びつける。

詳細な各ファイルの役割・拡張方法は `CLAUDE.md` を参照。

## 5. 既知の制約・限界

- **カタカナ表記は近似**: ㅓ/ㅗ、ㅡ/ㅜ、平音/激音の区別はカタカナで表現できない（`App.tsx`のフッター注記でユーザーにも明示済み）。正確な発音は必ずTTS再生で確認する前提の設計。
- **例外辞書は主に韓国料理系の定番語のみ**: `KNOWN_WORDS`には찌개・짜장면・김치・떡볶이・비빔밥・불고기・김밥・삼계탕・잡채・삼겹살などを登録済み。とはいえ網羅的ではないため、規則通りの変換が不自然になる語を見つけたら随時追加が必要。
- **連音はrun単位限定**: 空白・句読点をまたぐ音韻変化（複合語境界など）は扱えない仕様。
- **TTSは端末・ブラウザ依存**: Web Speech APIに韓国語（`ko-KR`）／日本語（`ja-JP`）音声がインストールされていない環境では再生できない。`voiceReady`/`hasKo`で警告文を出すのみで、代替手段（音声合成APIの外部利用など）は未実装。
- **テストはlibのみ**: `src/lib/`の各クラスに対するVitestのユニットテストは整備済み（`npm test`）だが、UI層（`App.tsx`／コンポーネント／フック）にはテストが無い。`LiaisonProcessor`はルールの優先順位に強く依存しているため、修正時はテストに加えて手動確認も推奨。
- **IME・特殊入力の挙動は未検証**: 結合文字や異体字など、標準的なハングル音節（U+AC00〜U+D7A3）以外の入力に対する挙動は確認されていない。

## 6. 今後の改善点（提案）

- `KNOWN_WORDS`辞書の拡充、または外部データ化（JSON分離など）による更新のしやすさ向上。
- 音節チップUIのアクセシビリティ改善（キーボード操作、スクリーンリーダー対応のaria属性など）。
- 再生速度（`rate`）以外の音声パラメータ（pitch等）調整UIの検討。
- TTS非対応環境向けの代替案（外部TTS APIの利用など）の検討。
